import React from 'react';

interface IconProps {
  className?: string;
}

export const NodeSkillIcon: React.FC<IconProps> = ({ className }) => {
  return (
    <svg 
      className={className}
      // SVG code will be added here
    >
    </svg>
  );
};
